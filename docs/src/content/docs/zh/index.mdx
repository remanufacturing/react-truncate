---
title: React Truncate
description: 提供 `Truncate`、`MiddleTruncate` 和 `ShowMore` React 组件，用于截断多行跨度并添加省略号。
template: splash
hero:
  tagline: 提供 `Truncate`、`MiddleTruncate` 和 `ShowMore` React 组件，用于截断多行跨度并添加省略号。
  image:
    html: <img class="w-full max-h-full" src="/logo.svg" />
  actions:
    - text: 开始
      link: /zh/guides/getting-started/
      icon: right-arrow
      variant: primary
---

import { Card, CardGrid } from '@astrojs/starlight/components'

## 开箱即用的组件

<CardGrid stagger>
  <Card title="ShowMore" icon="bars">
    带有 “展开” / “收起” 按钮的多行省略组件，可扩展更多的自定义功能。
  </Card>
  <Card title="MiddleTruncate" icon="add-document">
    比普通省略更有趣，可以指定位置以保留文本的首尾部分的内容，仅对中间内容处理省略的组件。
  </Card>
  <Card title="Truncate" icon="pencil">
    底层的基础组件，提供智能截短的功能处理，可根据您的业务需求封装更多的扩展能力。
  </Card>
</CardGrid>
